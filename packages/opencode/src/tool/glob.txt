- Fast file pattern matching tool that works with any codebase size
- Supports glob patterns like "**/*.js" or "src/**/*.ts"
- Returns matching file paths sorted by modification time
- Use this tool when you need to find files by name patterns
- When you are doing an open-ended search that may require multiple rounds of globbing and grepping, use the Task tool instead
- You have the capability to call multiple tools in a single response. It is always better to speculatively perform multiple searches as a batch that are potentially useful.

Output:
- No matches: "No files found"
- Matches: newline-separated absolute file paths sorted by modification time (newest first), capped at 100 results
- When truncated: appends "(Results are truncated. Consider using a more specific path or pattern.)"

Errors:
- File system or search process errors (e.g. invalid glob pattern, inaccessible directory)
